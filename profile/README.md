# TradeMarkManager - TMM


```bash
mkdir tmm
cd tmm
curl -fsSL https://raw.githubusercontent.com/trademark-manager/tmm/master/compose.yml -O
curl -fsSL https://raw.githubusercontent.com/trademark-manager/tmm/master/.env.sample -o .env
vim .env  # edit your config
docker-compose up -d
```

